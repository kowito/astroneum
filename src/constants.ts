/** Group ID used by the drawing-bar overlay manager. */
export const DRAWING_GROUP_ID = 'drawing_tools'

/**
 * Internal key used to store plugin runtime state in indicator.extendData.
 * Shared by plugin adapter + engine rendering path.
 */
export const INDICATOR_PLUGIN_RUNTIME_KEY = '__astroneumPluginRuntime'
